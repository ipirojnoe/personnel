<span class="user">
  <span class="user-avatar">
    <%= image_tag(user_avatar(user), class: 'img-icon-small') %>
  </span>
  <span class="user-name">
    <% if request.path == user_path(user) %>
      <%= user.username %>
    <% else %>
      <%= link_to user.username, user_path(user) %>
    <% end %>
  </span>
</span>
